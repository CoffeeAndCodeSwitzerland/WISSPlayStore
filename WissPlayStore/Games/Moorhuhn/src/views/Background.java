/*


import controllers.Moorhuhn;
import functions.Screenoptions;
import processing.core.PApplet;
import processing.core.PImage;

/**
 * Diese Klasse bestimmt wie das Hintergrundbild aussieht je nach easter-egg.
 * V1.0 / 20.04.2018
 * @author Patrick Bauersfeld
 *
 */
/*
public class Background {
	
	  PApplet parent;
	  PImage imgBackground;
	  
	  
	  public Background(PApplet p){
		parent = p;
		imgBackground = Moorhuhn.getMyImage().imgFieldBackground; //Ladet das Hintergrundbild
	  }
	  
	  public void drawBackground() {
		  checkState();
		  parent.setSize(parent.width, parent.height);
		  parent.background(imgBackground); //Zeichnet das Hintergrundbild
	  }
	  
	  public void checkState()
	  {
		  if (Moorhuhn.getMyGameController().myScore.score == 88) //Prüft ob der Score genau 88 beträgt, wenn ja dann wechselt das Hintergrundbild in die Normandie
		  {
			  Moorhuhn.getMyGameController().myClock.red = 0; //Setzt die Schriftfarbe auf Schwarz
			  Moorhuhn.getMyGameController().myClock.green = 0; //Setzt die Schriftfarbe auf Schwarz
			  Moorhuhn.getMyGameController().myClock.blue = 0; //Setzt die Schriftfarbe auf Schwarz
			  Moorhuhn.getMyGameController().myScore.red = 0; //Setzt die Schriftfarbe auf Schwarz
			  Moorhuhn.getMyGameController().myScore.green = 0; //Setzt die Schriftfarbe auf Schwarz
			  Moorhuhn.getMyGameController().myScore.blue = 0; //Setzt die Schriftfarbe auf Schwarz
		  }
	  }
}*/
